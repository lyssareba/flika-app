# Environment Strategy
# -------------------
# Locally:  Copy this file to .env and fill in your dev values.
# On EAS:   Set the same variable names as EAS Secrets (`eas secret:create`).
#           EAS Secrets override .env values at build time.
#
# Keys that differ between dev and production: all FIREBASE_* keys.
# APP_VARIANT is set automatically by eas.json build profiles — do not set it in EAS Secrets.

# Firebase Configuration
# Get these values from Firebase Console > Project Settings > Your Apps > Web App
FIREBASE_API_KEY=your-api-key
FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project.appspot.com
FIREBASE_MESSAGING_SENDER_ID=your-sender-id
FIREBASE_APP_ID=your-app-id

# RevenueCat Configuration
# Get these values from RevenueCat Dashboard > Project > API Keys
REVENUECAT_ANDROID_API_KEY=your-revenuecat-android-api-key
REVENUECAT_IOS_API_KEY=your-revenuecat-ios-api-key

# EAS Build Configuration
EAS_PROJECT_ID=your-eas-project-id
APP_VARIANT=development
